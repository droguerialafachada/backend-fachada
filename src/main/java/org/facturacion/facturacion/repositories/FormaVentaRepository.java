package org.facturacion.facturacion.repositories;

import org.facturacion.facturacion.domain.FormaVenta;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FormaVentaRepository extends JpaRepository<FormaVenta, Long> {
}
